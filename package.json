{
  "name": "ports-rs",
  "version": "0.2.5",
  "description": "Rust-powered CLI for inspecting and managing listening ports",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/EasyXdc/PortsWhisper-Rust.git"
  },
  "homepage": "https://github.com/EasyXdc/PortsWhisper-Rust",
  "bin": {
    "ports": "bin/ports.js",
    "whoisonport": "bin/whoisonport.js"
  },
  "files": [
    "assets",
    "bin",
    "scripts",
    "vendor",
    "README.md",
    "README.zh-CN.md"
  ],
  "scripts": {
    "test": "node --test tests/*.test.js",
    "postinstall": "node scripts/postinstall.js",
    "verify:release": "node scripts/verify-release.js",
    "homebrew:formula": "node scripts/homebrew-formula.js"
  },
  "engines": {
    "node": ">=18"
  }
}
